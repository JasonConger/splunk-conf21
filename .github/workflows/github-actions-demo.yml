name: GitHub Actions Demo
on:
  push:
    branches:
      - main
  repository_dispatch:
  
jobs:
  
  appinspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: package 
        uses: splunk/appinspect-cli-action@v1.1.0
        with:
          app-path: ./output/microsoft-azure-add-on-for-splunk_311.tgz
